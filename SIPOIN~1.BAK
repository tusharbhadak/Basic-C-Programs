// Pointer program in C

#include <stdio.h>
int main()
{
  int var = 5;
  int *p;
  clrscr();
  p = &var;
  printf("The value is: %d\n", var);

  // Notice the use of & before var
  printf("address of var: %p",p);
  getch();
  return 0;
}